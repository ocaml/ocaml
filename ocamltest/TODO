- Exécuter commandes dans environnemnet de ocamltest
- Redirections stdout (resp. stderr) à l'exécution:
  * Si $stdout (resp. $stderr) est défini on l'utilise
  * Sinon on redirige dans $program.output
  * Tenir compte de ce comportement pour la liste des fichiers générés

- Redirections lors de la compilation
  * Voir où et à quoi on compare
  * En tenir compte pour les fichiers générés

- Prévoir une option -log-commands
  * Si elle est définie, ce log fait-il partie des fichiers générés
    et donc à effacer si le test réussit?
